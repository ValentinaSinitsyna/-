function [ output_args ] = image_cut( image_full )
%cut input image to array of samples
%   input: image.png
%   output: 

sizes = size(S);
witdh = sizes(1);
height = sizes(2);


get



end


function [] = get_sample()
disp('hi')

end
